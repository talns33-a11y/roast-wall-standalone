---
description: 'A roast-app app.'
labels: ['app', 'react']
---

A React application for roast-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use roast-app 
```

To run the app locally, run the following:

```
bit run roast-app 
```
